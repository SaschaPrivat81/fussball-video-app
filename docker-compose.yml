services:
  u9-video:
    build: .
    container_name: u9-video
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_HOST:-127.0.0.1}:${PORT:-3001}:3001"
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
